{
  "name": "app-spa",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "deploy": "scp -o StrictHostKeyChecking=no -pr dist/* root@68.183.18.108:/var/www/wallet",
    "test": "vitest --run",
    "chrome": "$CHROME_EXECUTABLE --user-data-dir=/tmp/foo --ignore-certificate-errors --unsafely-treat-insecure-origin-as-secure=https://localhost/"
  },
  "dependencies": {
    "solid-js": "^1.7.8"
  },
  "devDependencies": {
    "vite": "^4.4.5",
    "vite-plugin-solid": "^2.7.0",
    "vitest": "^0.34.6",
    "@solidjs/testing-library": "^0.8.4",
    "happy-dom": "^12.10.3"
  }
}
